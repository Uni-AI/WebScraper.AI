Sets up each browser's Selenium Driver seperately!

Add yours to `my_browsers: {dict}` in `browser/defaults` to set which ones to install.

It autodetects your browser's version and installs the right one.