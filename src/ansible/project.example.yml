- name: Setup Web-Scraping Libraries
  hosts: localhost # , fedora, ubuntu
  vars:
    project: {
      "src_path": "PycharmProjects/LearnScraping",
    }

    # Host_vars
    ansible_become_password: u
    ansible_user: "{{lookup('ansible.builtin.env', 'USER')}}"
    #
    reinstall: false
    Debug: true

  roles:
    - Beautiful Soup
    - Selenium
